{
  "name": "floptwich",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "sass": "^1.69.5",
    "vite": "^4.4.5"
  },
  "dependencies": {
    "bootstrap": "^5.3.2"
  }
}
